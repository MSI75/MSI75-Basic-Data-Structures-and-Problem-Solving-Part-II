#include<bits/stdc++.h>
using namespace std;


int main(){
    // vector<int>a;

    // for(int i = 0; i<10; i++)
    //     a.push_back(i);
    
    // cout<<"Before "<<endl;
    // a.insert(a.begin()+2,100);
    // for(int i = 0; i<a.size(); i++)
    //     cout<<a[i]<<endl;
    
    // cout<<"After "<<endl;
    // a.erase(a.begin()+2);
    // for(int i = 0; i<a.size(); i++)
    //     cout<<a[i]<<endl;

    vector<int>a = {1,2,3,4,5,6};
    a.resize(3);
   
   for(int i = 0; i<a.size(); i++)
   cout<<a[i]<<endl;
}