#include <bits/stdc++.h>
using namespace std;

int main()
{
    /*
    // Time O(n^2)
    // Space O(1)
    int n; // O(1)
    cin >> n; // O(1)
    for (int i = 1; i < n; i++) // O(n*1)
    {
        cout << i << " "; // O(1)
    }

    for (int i = 1; i < n; i++) // O(n*n) = O(n^2)
    {
        for (int j = 1; j < n; j++) // O(n)
        {
            cout << i << " " << j << " "; // O(1)
        }
    }
    */

    /*
    // Time O(n+q)
    // space O(1)
       int n, q;
       cin>>n>>q;

       for(int i = 0; i<n; i++){ // O(n*1)
        cout<<i<<" "; // O(1)
       }

       for(int i = 0; i<q; i++){ // O(q*1)
        cout<<i<<" "; // O(1)
       }
    */

   /*
    //  Time O(n^3)
    // space O(1)
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) // O(n*n*n) = O(n^3)
    {
        for (int j = 0; j < n; j++) // O(n*n)
        {
            for (int k = 0; k < n; k++) // O(n*1)
            {
                cout << i << " " << j << " " << k << " "; // O(1)
            }
        }
    }
    */


    return 0;
}