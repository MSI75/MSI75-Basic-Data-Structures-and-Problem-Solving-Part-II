  // Stack<int> temp;

    // while (st.getSize() > 0)
    // {
    //     int t = st.top();
    //     st.pop();
    //     while (temp.getSize() > 0)
    //     {
    //         if (temp.top() < t)
    //         {
    //             break;
    //         }
    //         st.push(temp.top());
    //         temp.pop();
    //     }
    //     temp.push(t);
    // }

    // swap(st, temp);